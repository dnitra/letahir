interface TimeInputSettings {
    minMinutes: number;
    maxMinutes: number;
    minHour: number;
    maxHour: number;
    stepMinutes: number;
    defaultMinutes?: number;
}
export default TimeInputSettings;
